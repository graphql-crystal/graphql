name: Crystal CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: crystallang/crystal
    steps:
      - name: Install dependencies
        run: shards install
      - name: Run tests
        run: crystal spec
      - name: Build documentation
        run: crystal docs
